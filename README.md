# Flutter Getx 页面模板创建命令行工具

使用方法：

1、下载 newgetx或者newgetx.exe，授予执行权限，确保文件存放位置在环境变量中。

2、打开终端，在Flutter项目根目录中，输入：

    newgetx ./lib/模块名称

比如：

    newgetx ./lib/test2

它会创建一个名为test2的Getx 页面模板，类似 https://github.com/xdd666t/getx_template 这个插件做的事情。